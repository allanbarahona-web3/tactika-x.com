# 📊 ARQUITECTURA SMTP MULTI-TENANT - DIAGRAMA VISUAL

## 🔄 FLUJO DE CONFIGURACIÓN Y ENVÍO

```
┌─────────────────────────────────────────────────────────────────┐
│                     BARMENTECH OWNER                             │
│  (Accede a: POST /email/config/1)                               │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 │ {provider: "smtp", host: "smtp.gmail.com", ...}
                 ▼
         ┌───────────────────┐
         │  Email Controller │
         │ configureSmtp()   │
         └────────┬──────────┘
                  │
         ┌────────▼──────────────────┐
         │ 🔐 CryptoService.encrypt()│
         │ password → "encrypted:..." │
         └────────┬──────────────────┘
                  │
         ┌────────▼──────────────────────────────┐
         │ Prisma.tenant.update({                │
         │   config: {                           │
         │     email: {                          │
         │       provider: "smtp",               │
         │       host: "smtp.gmail.com",         │
         │       auth: {                         │
         │         pass: "encrypted:xxxxx"       │
         │       }                               │
         │     }                                 │
         │   }                                   │
         │ })                                    │
         └────────┬──────────────────────────────┘
                  │
         ┌────────▼──────────────────┐
         │  PostgreSQL BD            │
         │  tenants.config (JSON)    │
         │  ✅ Config Guardada       │
         └──────────────────────────┘


```

---

## 📤 FLUJO DE ENVÍO DE EMAIL

```
┌────────────────────────────────────────────────────────────────┐
│ EVENT: Nueva Orden Creada                                      │
│ Order #1001 para customer@example.com, Tenant ID: 1           │
└────────────────┬──────────────────────────────────────────────┘
                 │
         ┌───────▼────────────────────────┐
         │ OrdersService.createOrder()    │
         │ ↓                              │
         │ emailService.sendEmail({       │
         │   tenantId: 1,                 │
         │   to: "customer@example.com",  │
         │   subject: "Order Confirmed",  │
         │   html: "<h1>Order #1001</h1>" │
         │ })                             │
         └───────┬────────────────────────┘
                 │
         ┌───────▼──────────────────────────────┐
         │ EmailService.sendEmail()             │
         │ 1. Obtener tenant config de BD       │
         │ 2. SELECT * FROM tenants             │
         │    WHERE id = 1                      │
         └───────┬──────────────────────────────┘
                 │
         ┌───────▼────────────────────────────────────┐
         │ getOrCreateTransporter(tenantId: 1,        │
         │                         emailConfig: {...}) │
         │ 1. Buscar en cache (Map)                   │
         │ 2. Si no existe:                           │
         │    ↓                                        │
         │    createSmtpTransporter()                 │
         │      - Desencriptar password               │
         │      - Crear nodemailer.Transporter        │
         │      - Guardar en cache                    │
         └───────┬──────────────────────────────────┘
                 │
         ┌───────▼──────────────────────────────┐
         │ transporter.sendMail({               │
         │   from: "noreply@barmentech.com",    │
         │   to: "customer@example.com",        │
         │   subject: "Order Confirmed",        │
         │   html: "<h1>Order #1001</h1>"       │
         │ })                                   │
         └───────┬──────────────────────────────┘
                 │
         ┌───────▼──────────────────────────────┐
         │ 📧 SMTP GATEWAY (Gmail, SendGrid, etc)   │
         │ ✅ Email Enviado                     │
         └───────┬──────────────────────────────┘
                 │
         ┌───────▼──────────────────────┐
         │ customer@example.com         │
         │ ✅ Email Recibido            │
         └──────────────────────────────┘
```

---

## 🗄️ ESTRUCTURA DE DATOS EN BD

```sql
-- tenants table
┌─────────────────────────────────────────────────────────┐
│ id  │ name        │ config (JSON)                       │
├─────┼─────────────┼─────────────────────────────────────┤
│ 1   │ Barmentech  │ {                                   │
│     │             │   "email": {                        │
│     │             │     "provider": "smtp",             │
│     │             │     "host": "smtp.gmail.com",       │
│     │             │     "port": 587,                    │
│     │             │     "auth": {                       │
│     │             │       "user": "admin@barmentech.com"│
│     │             │       "pass": "encrypted:xxx..."    │
│     │             │     },                              │
│     │             │     "fromAddress": "noreply@...",   │
│     │             │     "fromName": "Barmentech",       │
│     │             │     "isActive": true                │
│     │             │   },                                │
│     │             │   "timezone": "America/El_Salvador",│
│     │             │   "currency": "USD"                 │
│     │             │ }                                   │
├─────┼─────────────┼─────────────────────────────────────┤
│ 2   │ ARMAS       │ {                                   │
│     │             │   "email": {                        │
│     │             │     "provider": "sendgrid",         │
│     │             │     "apiKey": "encrypted:SG.xxx...",│
│     │             │     "fromAddress": "noreply@...",   │
│     │             │     "fromName": "ARMAS",            │
│     │             │     "isActive": true                │
│     │             │   }                                 │
│     │             │ }                                   │
└─────┴─────────────┴─────────────────────────────────────┘
```

---

## 🔐 ENCRIPTACIÓN DE SECRETS

```
┌──────────────────────────────────────┐
│ ADMINISTRADOR CONFIGURA              │
│ password: "mi_contrasena_real"       │
└──────────────┬───────────────────────┘
               │
        ┌──────▼─────────────────┐
        │ CryptoService          │
        │ .encrypt(password)     │
        └──────┬─────────────────┘
               │
        ┌──────▼──────────────────────────┐
        │ "encrypted:xxxxxxxxxxxxx"       │
        │ (usando AES-256-GCM)            │
        └──────┬──────────────────────────┘
               │
        ┌──────▼──────────────────────────┐
        │ Base de Datos PostgreSQL        │
        │ tenants.config JSON             │
        │ ✅ Almacenado encriptado        │
        └──────┬──────────────────────────┘
               │
        ┌──────▼──────────────────┐
        │ Al enviar email:        │
        │ .decrypt(password)      │
        │ → "mi_contrasena_real"  │
        │ ✅ Usa credencial real  │
        └─────────────────────────┘
```

---

## 🎯 MATRIZ DE PERMISOS (RLS)

```
┌─────────────────────────────────────────────────────────────┐
│                    BARMENTECH OWNER                          │
└─────────────────────────────────────────────────────────────┘

  CAN:
  ✅ POST   /email/config/1       (configurar SMTP)
  ✅ GET    /email/config/1       (ver config sin password)
  ✅ POST   /email/verify/1       (verificar SMTP)
  ✅ POST   /email/test/1         (enviar test email)
  ✅ POST   /email/disable/1      (desactivar SMTP)

  CANNOT:
  ❌ POST   /email/config/2       (no es propietario de tenant 2)
  ❌ GET    /email/config/2       (no puede ver config de otro)
  ❌ Ver password real en responses (siempre oculto con ***)

┌─────────────────────────────────────────────────────────────┐
│                    STAFF MEMBER (Barmentech)                │
└─────────────────────────────────────────────────────────────┘

  CANNOT:
  ❌ POST   /email/config/1       (solo owner)
  ❌ GET    /email/config/1       (solo owner)
  ❌ POST   /email/verify/1       (solo owner)
```

---

## 📊 CASOS DE USO POR MÓDULO

```
┌─────────────────────────────────────────────────────────┐
│ ORDERS MODULE                                           │
├─────────────────────────────────────────────────────────┤
│ ✅ Confirmación de Pedido                              │
│    trigger: POST /orders                               │
│    email: "Order #{orderNumber} confirmed"             │
│                                                         │
│ ✅ Cambio de Estado (shipped, delivered)               │
│    trigger: PATCH /orders/{id}/status                  │
│    email: "Your order is on the way!"                  │
│                                                         │
│ ✅ Cancelación de Pedido                               │
│    trigger: DELETE /orders/{id}                        │
│    email: "Order cancelled"                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ PAYMENTS MODULE                                         │
├─────────────────────────────────────────────────────────┤
│ ✅ Confirmación de Pago                                │
│    trigger: POST /payments (status: paid)              │
│    email: "Payment received"                           │
│                                                         │
│ ✅ Fallo de Pago                                       │
│    trigger: POST /payments (status: failed)            │
│    email: "Payment failed, try again"                  │
│                                                         │
│ ✅ Reembolso Procesado                                 │
│    trigger: PATCH /payments/{id}/refund               │
│    email: "Refund processed"                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ AUTH MODULE                                             │
├─────────────────────────────────────────────────────────┤
│ ✅ Bienvenida Nuevo Usuario                            │
│    trigger: POST /auth/register                        │
│    email: "Welcome to {storeName}"                     │
│                                                         │
│ ✅ Reset Contraseña                                    │
│    trigger: POST /auth/forgot-password                 │
│    email: "Reset your password"                        │
│                                                         │
│ ✅ Login desde nuevo dispositivo                       │
│    trigger: POST /auth/login                          │
│    email: "New login detected"                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ TENANT-USERS MODULE                                     │
├─────────────────────────────────────────────────────────┤
│ ✅ Nuevo Staff Invitado                                │
│    trigger: POST /tenant-users                         │
│    email: "You have been invited to {storeName}"       │
│                                                         │
│ ✅ Role Cambió                                         │
│    trigger: PATCH /tenant-users/{id}/role              │
│    email: "Your role has been updated"                 │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 FLUJO COMPLETO: De Configuración a Envío

```
PASO 1: BARMENTECH ADMIN CONFIGURA SMTP
────────────────────────────────────────
POST /email/config/1
Body: {
  provider: "smtp",
  host: "smtp.gmail.com",
  port: 587,
  auth: { user: "admin@barmentech.com", pass: "app_password" }
}
       │
       ├─ ✅ Valida que es owner
       ├─ ✅ Encripta password
       ├─ ✅ Actualiza tenants.config JSON
       ├─ ✅ Limpia cache
       └─ Response: { success: true }


PASO 2: CLIENTE REALIZA COMPRA
──────────────────────────────
POST /orders
Body: { productIds: [1, 2, 3], customerId: "cust123" }
       │
       ├─ ✅ Crea orden en BD
       ├─ ✅ emailService.sendEmail({
       │        tenantId: 1,
       │        to: customer.email,
       │        subject: "Order Confirmed",
       │        html: "<h1>Thanks for your order!</h1>"
       │      })
       └─ Response: { orderId: "ord456" }


PASO 3: EMAIL SERVICE ENVÍA EMAIL
──────────────────────────────────
emailService.sendEmail()
       │
       ├─ ✅ Obtiene config de tenant 1
       ├─ ✅ Busca/crea transporter en cache
       ├─ ✅ Desencripta password
       ├─ ✅ Conecta a SMTP
       ├─ ✅ Envía email
       └─ Response: { messageId: "msg123", success: true }


PASO 4: CLIENTE RECIBE EMAIL
─────────────────────────────
Customer@example.com
       │
       └─ ✅ 📧 "Order #1001 Confirmed"
```

---

## 📈 ESCALABILIDAD

```
CON 100 TENANTS

┌─────────────────────────────────────┐
│  TENANT 1 (Barmentech) - SMTP Gmail │  → emails/día: 1000+
│  config.email = { provider: smtp... }
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  TENANT 2 (ARMAS) - SendGrid        │  → emails/día: 500+
│  config.email = { provider: sendgrid...}
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  TENANT 3 (TechStore) - Mailgun     │  → emails/día: 2000+
│  config.email = { provider: mailgun...}
└─────────────────────────────────────┘

...

┌──────────────────────────────────────────────┐
│  ✅ Cada tenant independiente                │
│  ✅ Falla de un tenant no afecta a otros    │
│  ✅ Cache evita queries repetidas            │
│  ✅ RLS aísla datos completamente            │
└──────────────────────────────────────────────┘
```

---

## 🎓 CONCLUSIÓN

**Arquitectura implementada:**
- ✅ Configuración SMTP por tenant
- ✅ Soporte múltiples proveedores (SMTP, SendGrid, Mailgun, AWS SES)
- ✅ Encriptación de credentials
- ✅ Caché en memoria (24h TTL)
- ✅ RLS para aislamiento de datos
- ✅ 5 endpoints REST completos
- ✅ Rate limiting específico
- ✅ Validación robusta

**Listo para producción con 100+ dominios** ✅

